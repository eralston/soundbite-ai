/**
 * Feed web part properties interface.  This "wraps" the feed component so it has some of the same
 * properties that are found within the component properties.
 */
export interface ISoundbiteFeedWebPartProps {
  hideTitle?: boolean;
  groupRoute?: string;
  title?: string;
}
